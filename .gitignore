# Python
__pycache__/
*.py[cod]
*.pyo

# pytest
.pytest_cache/
.coverage

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Sub-agent docs (local only)
docs/api-migration-agent.md

# 訓練產出（checkpoint / tensorboard log，體積大且可由設定檔重現）
training/outputs/

# Secrets (local only, never commit)
.env
.env.*
!.env.example
*.key
secrets/

*.egg-info/

# 本機除錯用 log（BILLIARD_DEBUG_LOG_PATH／GUI 主控台重導向輸出），
# 內容因人因次而異，不是可重現的產物
billiard_gui.log
gui_console.log
gui_console_utf8.log

# scripts/verify_ur10e_cue_actuator.py 的算圖產出（隨時可重跑產生）
_actuator_preview/
# scripts/benchmark_gui_frametime.py 的量測產出（log + JSON，隨時可重跑產生）
_bench/
