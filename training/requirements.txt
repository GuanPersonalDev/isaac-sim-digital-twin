# 訓練專用依賴。Isaac Sim 映像已內含 torch / numpy / Isaac Sim runtime，
# 這裡只列額外需要的套件，不要重複釘住 torch 版本（會跟映像內的 CUDA build 打架）。
#
# TODO: RL 框架尚未定案，確定後補上並釘版本。
#   - Isaac Lab 走 source 安裝，不是 pip package，需另在 Dockerfile 加 clone 步驟
#   - 若用 skrl / rl_games / stable-baselines3，在此列出

pyyaml>=6.0
tensorboard>=2.16
