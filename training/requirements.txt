# 訓練專用的額外依賴。
#
# torch / numpy / isaacsim / isaaclab 由 training/scripts/bootstrap_runpod.sh
# 裝進 /workspace/venv，**不要**在這裡重複釘住版本——尤其是 torch，釘錯會把
# 已驗證的 cu128 堆疊換掉（見 training/README.md〈踩過的坑〉）。
#
# TODO: RL 框架尚未定案，確定後補上並釘版本（rsl_rl 走 Isaac Lab 的安裝流程，
#       不是 pip package；若改用 skrl / rl_games / stable-baselines3 則列在此）。

pyyaml>=6.0
tensorboard>=2.16
